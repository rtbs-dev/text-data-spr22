# to use: 
# `conda env update --file env.yaml`
name: text-data-class
channels:
  - conda-forge
dependencies:
  - python=3.9
  - ipykernel
  - dvc
  - numpy
  - seaborn
  - plotnine
  - scikit-learn
  - pandas
  - pyarrow
  - pip
  - pip:
    - datatest
    - coconut-develop[kernel]
    - bertopic
 